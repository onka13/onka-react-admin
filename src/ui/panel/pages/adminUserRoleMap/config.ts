import { PageConfig } from 'onka-react-admin-core';

export const pageConfig = new PageConfig({
  route: '/AdminApi/RoleMap',
  moduleKey: 'AdminApi',
  pageKey: 'RoleMap',
  menu: 'AdminApi',
  menuOrder: 0,
});
